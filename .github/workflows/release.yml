name: Release

# Release builds are done locally (sign + notarize + DMG).
# This workflow is kept as a placeholder for future CI restoration.
# See docs/release-guide.md for the local release process.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g. v1.7.0)'
        required: true

permissions:
  contents: write

jobs:
  placeholder:
    name: Release (local build required)
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "============================================"
          echo "Release builds are done locally."
          echo "See docs/release-guide.md for instructions."
          echo "============================================"
          echo ""
          echo "To release:"
          echo "  1. Run tests locally"
          echo "  2. xcodebuild archive → codesign → notarize → staple → DMG"
          echo "  3. gh release create vX.Y.Z OpenPaste-X.Y.Z.dmg --notes-file RELEASE_NOTES.md"
          echo "  4. Update homebrew-tap/Casks/openpaste.rb"
          echo "  5. Update appcast.xml on gh-pages"
